name: Build ARM Binaries

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Backend
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 拉取官方前端源码
      - name: Checkout Frontend
        uses: actions/checkout@v4
        with:
          repository: OpenListTeam/OpenList-Frontend
          path: frontend-src
          ref: main 

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Build Frontend with Chinese
        run: |
          cd frontend-src
          echo "📥 安装前端依赖..."
          pnpm install --no-frozen-lockfile

          echo "🇨🇳 [Fix 1] 尝试生成语言包..."
          # 尝试运行可能存在的 i18n 脚本
          npm run i18n || node scripts/i18n.mjs || echo "跳过标准 i18n 脚本"

          echo "🇨🇳 [Fix 2] 强制修改默认语言为简体中文 (zh-CN)..."
          # 使用 sed 命令修改源码中的默认配置
          # 将 fallbackLng: "en" 改为 "zh-CN"
          # 将 wait: 'en' 改为 'zh-CN'
          if [ -f "src/app/i18n.ts" ]; then
            sed -i 's/fallbackLng: "en"/fallbackLng: "zh-CN"/g' src/app/i18n.ts
            sed -i "s/wait: 'en'/wait: 'zh-CN'/g" src/app/i18n.ts
            echo "✅ 已修改 src/app/i18n.ts"
          else
            echo "⚠️ 未找到 i18n.ts 文件，跳过修改"
          fi

          echo "🏗️ 开始构建前端..."
          # CI=false 防止因警告停止构建
          CI=false pnpm build

          echo "📂 移动构建产物..."
          cd ..
          mkdir -p public/dist
          rm -rf public/dist/*
          cp -r frontend-src/dist/* public/dist/

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Install Cross-Compilers
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu gcc-arm-linux-gnueabihf

      - name: Build for Linux ARM64
        env:
          CGO_ENABLED: 1
          GOOS: linux
          GOARCH: arm64
          CC: aarch64-linux-gnu-gcc
        run: |
          go build -ldflags "-w -s" -o openlist-linux-arm64 main.go

      - name: Build for Linux ARMv7
        env:
          CGO_ENABLED: 1
          GOOS: linux
          GOARCH: arm
          GOARM: 7
          CC: arm-linux-gnueabihf-gcc
        run: |
          go build -ldflags "-w -s" -o openlist-linux-arm-v7 main.go

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: openlist-arm-binaries
          path: |
            openlist-linux-arm64
            openlist-linux-arm-v7
